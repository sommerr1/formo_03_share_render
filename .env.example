# Придумай сам: длинная случайная строка (≥32 символа). Тот же secret — в Formo.
# Пример генерации: openssl rand -hex 32
ADMIN_SECRET=change-me-in-netlify-env
ALLOWED_ORIGIN=*
# Kill-switch without code rollback: false | 0 | off
VISIT_ANALYTICS_ENABLED=true
